steamLanguage="";
language="English";
adapter=-1;
3D_Performance=1;
Resolution_W=800;
Resolution_H=600;
Resolution_Bpp=32;

// Can't make MTU 1480 because I can't connect to that

class sockets {
    // Packet MTU, keep lower than 1500 to not risk packet fragmentation, default 1400
    maxPacketSize = 1444;
    // Initial negotiated client connection speed in bytes, default 32000 (256 kbit)
    initBandwidth = 2000000; // 16 mbit
    // Minimal negotiated client connection speed in bytes, default 8000 (64 kbit)
    MinBandwidth = 250000; // 2 mbit
    // Maximal negotiated client connection speed in bytes, default 2000000 (16 Mbit)
    MaxBandwidth = 3000000; // 24 mbit
};

maxMsgSend = 256; // default is 128
maxSizeGuaranteed = 1024; // default is 512
maxSizeNonguaranteed = 512; // default is 256
minBandwidth = 200000000; // 200 Megabit
maxBandwidth = 1000000000; // 1 Gigabit
minErrorToSend = 0.002;
minErrorToSendNear = 0.01;
maxCustomFileSize = 0; // Disable custom faces
